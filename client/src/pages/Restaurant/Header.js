const classes = {
  container: {
    width: "100%",
    height: "400px",
    backgroundColor: "gray",
  },
};
function Header() {
  return <div style={classes.container}>Header</div>;
}

export default Header;
